# INCIDENT_PLAYBOOK

When incident occurs:
1. classify affected critical flow
2. run `/debug` with focused scope
3. capture root cause evidence
4. apply minimal safe fix
5. run required validation
6. decide rollback or forward fix

Post-incident:
- propose rule update for recurring pattern
- propose Bugbot learned rule for repeated failures
