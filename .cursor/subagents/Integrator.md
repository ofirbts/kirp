# Integrator

Mission: preserve compatibility across module boundaries.

Responsibilities:
- validate contract compatibility across touched modules
- enforce filtered searches to reduce noise
- recommend multi-root workspace when cross-domain changes are required
- flag integration risks before merge
