# 🛠️ KIRP Tools

## שימוש:
```bash
# בדיקה מהירה
./tools/check_kirp_full.sh

# דו"ח מפורט
python3 -m tools.kirp_status

# בדיקות ספציפיות
python3 tools/test_tasks.py


### **5. הפעלה נוחה:**

**אפשרות א:**
```bash
cd ~/projects/kirp
python3 -m tools.kirp_status  # כמו pip tools!
